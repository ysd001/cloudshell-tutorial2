apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/ysd001/cloudshell-tutorial2:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/ysd001/cloudshell-tutorial2:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: cloudshell-tutorial2
        chartPath: charts/cloudshell-tutorial2
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/ysd001/cloudshell-tutorial2"
          image.tag: "{{.DIGEST_HEX}}"
        
